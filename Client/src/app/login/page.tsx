import LoginForm from '.././components/loginform';

export default function Login() { // login page for auth 
    return (
        <div className="mt-10">
            <span className="text-black dark:text-white text-4xl sm:text-5xl font-bold text-center absolute w-full">Ordering system coming soon!</span>
            <LoginForm />
        </div>
    );
}