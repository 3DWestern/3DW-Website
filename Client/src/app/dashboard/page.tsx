import React from 'react';

// TODO: Do I need 2 dashboards then? 
// I can have the server return a single dashboard data? 
export default async function Dashboard () {
    return (
        <div>
            <h1>Dashboard</h1>
        </div>
    );
}