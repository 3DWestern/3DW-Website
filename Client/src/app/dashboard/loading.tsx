// file for streaming; send partial HTML before all data/comps are ready
// shows fallback while route segment pending 
export default function Loading() { // TODO: replace this with an element instead of text later! 
    return(
        <div>Loading...</div>
    );
}